[package]
name = "resonant-cli"
description = "Operator CLI for the resonant membership reference kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "resonant"
path = "src/main.rs"

[dependencies]
resonant-kernel = { path = "../resonant-kernel" }
resonant-lab = { path = "../resonant-lab" }
clap = { workspace = true }
serde_json = { workspace = true }
